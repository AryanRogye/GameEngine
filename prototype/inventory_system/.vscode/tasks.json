{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with compile.sh",
            "type": "shell",
            "command": "${workspaceFolder}/compile.sh",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
